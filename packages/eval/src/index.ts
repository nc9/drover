// @droveragent/eval — reserved.
// Formal ScenarioRunner / Scorer / Reporter library. The in-repo eval
// suite (`evals/run.ts`) is the current source of truth; this package
// will host the reusable runner once the API stabilises.
export {};
