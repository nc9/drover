{
  "name": "droveragent-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "evals", "apps/*"],
  "scripts": {
    "format": "oxfmt packages",
    "lint": "oxlint packages",
    "test": "bun run --filter '*' --if-present test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@typescript/native-preview": "latest",
    "oxfmt": "latest",
    "oxlint": "latest",
    "typescript": "^5.6.0"
  }
}
